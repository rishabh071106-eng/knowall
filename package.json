{
  "name": "knowall",
  "version": "0.1.0",
  "private": true,
  "description": "Knowall — learn anything for ₹10",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "build": "npm --prefix frontend run build && rm -rf backend/public && mv frontend/dist backend/public",
    "start": "cd backend && NODE_ENV=production node src/index.js",
    "migrate": "psql \"$DATABASE_URL\" -f backend/src/db/schema.sql",
    "seed": "psql \"$DATABASE_URL\" -f backend/src/db/seed.sql",
    "build-and-start": "npm run build && npm start"
  }
}
