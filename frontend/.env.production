# Production — frontend and backend served from the same origin,
# so API base is a relative path.
VITE_API_BASE=/api
# Razorpay key id filled in at build time on your host's dashboard.
VITE_RAZORPAY_KEY_ID=
